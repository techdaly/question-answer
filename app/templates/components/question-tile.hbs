<li>{{#link-to 'question' question.id}}
  <hr>
  <div id="q-title">{{question.title}}</div><br>
  {{/link-to}}
  <div id="q-asker">{{question.asker}}</div><br><br>

  {{#if isQTextShowing }}
    <p {{action 'qtextHide'}}>{{question.qtext}}</p>
  {{else}}
    <button {{action 'qtextShow'}}>Show Question</button>
  {{/if}}
</li>
